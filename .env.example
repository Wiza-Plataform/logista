# Copiar para .env.local (nunca commitar o real — SEC-022)

# API da WIZA (repo `api`)
API_BASE_URL=http://localhost:3000

# Sessão: cookies HttpOnly emitidos pela API — nunca guardar tokens no localStorage (SEC-*)
